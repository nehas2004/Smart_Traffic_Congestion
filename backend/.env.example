# Copy this file to .env and fill in your real keys.
# NEVER commit .env to version control.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# SQLite (default) — swap to postgres:// for production
DATABASE_URL=sqlite:///./ai_module.db
